# 如何获取扣子站点访问令牌

## 什么是扣子站点访问令牌？

扣子（Coze）是字节跳动推出的AI智能体开发平台。我们的装修决策Agent使用了扣子平台部署的AI智能体来分析报价单、合同和验收照片。

**扣子站点访问令牌**是用于访问已发布到站点的智能体的认证令牌。它允许我们的后端服务通过API调用扣子智能体。

## 当前配置信息

从生产环境配置中可以看到：
- **扣子站点URL**: `https://9n37hmztzw.coze.site`
- **项目ID**: `7603691852046368804`
- **当前令牌**: 已过期（HTTP 403错误）

## 获取新令牌的步骤

### 步骤1：登录扣子平台
1. 访问扣子官网：https://www.coze.cn
2. 使用账号登录（需要是智能体的创建者或管理员）

### 步骤2：进入智能体管理
1. 登录后进入控制台
2. 找到"装修决策Agent"智能体（或类似的智能体名称）
3. 点击进入智能体编辑页面

### 步骤3：发布到站点（如果尚未发布）
如果智能体尚未发布到站点，需要先发布：
1. 在智能体编辑页面，点击右上角的"发布"按钮
2. 选择"发布到站点"
3. 配置站点信息（可以保持默认）
4. 完成发布，获取站点URL

### 步骤4：获取站点访问令牌
1. 在智能体管理页面，找到"站点集成"或"API访问"选项
2. 点击"生成访问令牌"或"获取Bearer Token"
3. 复制生成的令牌（通常以`eyJ`开头的JWT令牌）

**注意**：扣子平台可能有不同的界面，具体位置可能是：
- 智能体设置 → 高级设置 → API访问
- 发布管理 → 站点设置 → 访问令牌
- 集成中心 → 站点API → 令牌管理

### 步骤5：验证令牌有效性
获取新令牌后，可以使用以下命令测试：
```bash
# 测试新令牌
curl -X POST "https://9n37hmztzw.coze.site/stream_run" \
  -H "Authorization: Bearer 新令牌" \
  -H "Content-Type: application/json" \
  -d '{
    "type": "query",
    "session_id": "test_session_123",
    "project_id": "7603691852046368804",
    "content": {
      "query": {
        "prompt": [
          {
            "type": "text",
            "content": {
              "text": "测试连接，请回复'连接成功'"
            }
          }
        ]
      }
    }
  }' \
  --max-time 30
```

如果返回正常响应（不是403错误），说明令牌有效。

## 更新生产环境配置

### 方法1：手动更新
```bash
# 备份当前配置
ssh -i ~/zhuangxiu-agent1.pem root@120.26.201.61 "cd /root/project/prod/zhuangxiu-agent && cp .env.prod .env.prod.backup.$(date +%Y%m%d_%H%M%S)"

# 更新扣子站点令牌
ssh -i ~/zhuangxiu-agent1.pem root@120.26.201.61 "cd /root/project/prod/zhuangxiu-agent && sed -i 's/COZE_SITE_TOKEN=.*/COZE_SITE_TOKEN=新令牌/' .env.prod"

# 验证更新
ssh -i ~/zhuangxiu-agent1.pem root@120.26.201.61 "cd /root/project/prod/zhuangxiu-agent && grep COZE_SITE_TOKEN .env.prod"
```

### 方法2：直接编辑配置文件
```bash
# SSH登录服务器
ssh -i ~/zhuangxiu-agent1.pem root@120.26.201.61

# 进入项目目录
cd /root/project/prod/zhuangxiu-agent

# 编辑配置文件
nano .env.prod

# 找到COZE_SITE_TOKEN行，更新为：
COZE_SITE_TOKEN=新令牌

# 保存并退出（Ctrl+X，然后Y，然后Enter）
```

## 同时更新DeepSeek API密钥

由于DeepSeek API密钥也无效，建议同时更新：

### 获取DeepSeek API密钥
1. 访问DeepSeek平台：https://platform.deepseek.com
2. 登录账号
3. 进入API密钥管理
4. 创建新的API密钥或使用现有有效密钥

### 更新DeepSeek配置
```bash
# 更新DeepSeek API密钥
ssh -i ~/zhuangxiu-agent1.pem root@120.26.201.61 "cd /root/project/prod/zhuangxiu-agent && sed -i 's/DEEPSEEK_API_KEY=.*/DEEPSEEK_API_KEY=新DeepSeek密钥/' .env.prod"
```

## 重启服务

更新配置后需要重启后端服务：
```bash
# 重启后端容器
ssh -i ~/zhuangxiu-agent1.pem root@120.26.201.61 "cd /root/project/prod/zhuangxiu-agent && docker compose restart backend"

# 查看重启日志
ssh -i ~/zhuangxiu-agent1.pem root@120.26.201.61 "cd /root/project/prod/zhuangxiu-agent && docker compose logs backend --tail=20"
```

## 验证修复

### 验证1：检查服务状态
```bash
# 检查后端服务是否正常运行
ssh -i ~/zhuangxiu-agent1.pem root@120.26.201.61 "cd /root/project/prod/zhuangxiu-agent && docker compose ps backend"
```

### 验证2：运行测试脚本
```bash
# 在本地运行测试脚本
cd /Users/mac/zhuangxiu-agent
python3 test_quote_analysis_full.py
```

### 验证3：查看日志确认
```bash
# 查看扣子API调用是否成功
ssh -i ~/zhuangxiu-agent1.pem root@120.26.201.61 "cd /root/project/prod/zhuangxiu-agent && docker compose logs backend --tail=30 | grep -E '扣子站点API|AI分析成功'"
```

## 常见问题

### Q1：找不到扣子站点令牌选项怎么办？
A：扣子平台界面可能更新，可以尝试：
1. 查看智能体的"设置"或"高级设置"
2. 查找"API"、"集成"、"Webhook"等相关选项
3. 联系扣子平台客服或查看文档

### Q2：令牌有效期是多久？
A：扣子站点令牌通常有有效期，可能是：
- 永久有效（除非手动撤销）
- 有一定期限（如30天、90天）
- 建议定期检查并更新

### Q3：如果扣子智能体被删除或修改了怎么办？
A：如果原来的智能体不可用，需要：
1. 重新创建智能体
2. 配置相同的分析功能
3. 重新发布到站点
4. 获取新令牌

### Q4：有没有备用方案？
A：是的，可以：
1. 使用DeepSeek API作为主要服务（需要有效API密钥）
2. 配置其他AI服务（如OpenAI、通义千问等）
3. 临时启用本地OCR分析（质量较低）

## 联系支持

如果遇到问题，可以联系：
- **扣子平台支持**：通过扣子官网客服或社区
- **DeepSeek支持**：通过DeepSeek平台客服
- **项目技术负责人**：负责AI服务集成

## 重要提醒

1. **安全保存令牌**：API令牌是敏感信息，不要泄露
2. **定期检查**：建议每月检查一次AI服务配置
3. **监控告警**：设置AI分析失败告警，及时发现问题
4. **备份配置**：更新前备份配置文件

---

**最后更新**：2026-03-17  
**相关文档**：
- `报价单分析功能问题排查与修复方案.md`
- `报价单分析功能排查步骤与日志命令.md`
- `test_quote_analysis_full.py`
