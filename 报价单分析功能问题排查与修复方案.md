# 报价单分析功能问题排查与修复方案

## 问题概述

**问题类型**：后台问题

**影响范围**：报价单分析功能完全不可用

**根本原因**：AI分析服务配置失效
1. 扣子站点API令牌过期（HTTP 403）
2. DeepSeek API备用服务密钥无效（HTTP 401）

## 详细错误日志

### 1. 扣子站点API错误
```
2026-03-17 08:03:27,523 - httpx - INFO - HTTP Request: POST https://9n37hmztzw.coze.site/stream_run "HTTP/1.1 403 Forbidden"
2026-03-17 08:03:27,524 - app.services.coze_service - ERROR - 扣子站点API HTTP错误: 403 -
```

### 2. DeepSeek API错误
```
2026-03-17 08:03:27,695 - httpx - INFO - HTTP Request: POST https://api.deepseek.com/v1/chat/completions "HTTP/1.1 401 Unauthorized"
2026-03-17 08:03:27,697 - app.services.coze_service - ERROR - DeepSeek API调用异常: Error code: 401 - {'error': {'message': 'Authentication Fails, Your api key: ****v4w5 is invalid', 'type': 'authentication_error', 'param': None, 'code': 'invalid_request_error'}}
```

## 排查步骤

### 步骤1：检查当前配置
```bash
# 查看生产环境配置
ssh -i ~/zhuangxiu-agent1.pem root@120.26.201.61 "cd /root/project/prod/zhuangxiu-agent && grep -E 'COZE_SITE_TOKEN|DEEPSEEK_API_KEY' .env.prod"
```

### 步骤2：测试扣子站点连接
```bash
# 测试扣子站点API
curl -X POST "https://9n37hmztzw.coze.site/stream_run" \
  -H "Authorization: Bearer [当前令牌]" \
  -H "Content-Type: application/json" \
  -d '{"type":"query","session_id":"test_session","project_id":"7603691852046368804","content":{"query":{"prompt":[{"type":"text","content":{"text":"测试"}}]}}}'
```

### 步骤3：测试DeepSeek API
```bash
# 测试DeepSeek API
curl -X POST "https://api.deepseek.com/v1/chat/completions" \
  -H "Authorization: Bearer [当前密钥]" \
  -H "Content-Type: application/json" \
  -d '{"model":"deepseek-chat","messages":[{"role":"user","content":"Hello"}]}'
```

## 修复方案

### 方案1：更新扣子站点令牌

1. **登录扣子平台**：访问 https://www.coze.cn
2. **进入智能体管理**：找到"装修决策Agent"智能体
3. **获取新令牌**：
   - 进入智能体设置
   - 找到"API访问"或"站点集成"选项
   - 生成新的访问令牌
4. **更新配置**：
   ```bash
   # 备份当前配置
   ssh -i ~/zhuangxiu-agent1.pem root@120.26.201.61 "cd /root/project/prod/zhuangxiu-agent && cp .env.prod .env.prod.backup.$(date +%Y%m%d)"
   
   # 更新扣子站点令牌
   ssh -i ~/zhuangxiu-agent1.pem root@120.26.201.61 "cd /root/project/prod/zhuangxiu-agent && sed -i 's/COZE_SITE_TOKEN=.*/COZE_SITE_TOKEN=新令牌/' .env.prod"
   ```

### 方案2：更新DeepSeek API密钥

1. **登录DeepSeek平台**：访问 https://platform.deepseek.com
2. **获取API密钥**：
   - 进入API密钥管理
   - 创建新的API密钥或使用现有有效密钥
3. **更新配置**：
   ```bash
   # 更新DeepSeek API密钥
   ssh -i ~/zhuangxiu-agent1.pem root@120.26.201.61 "cd /root/project/prod/zhuangxiu-agent && sed -i 's/DEEPSEEK_API_KEY=.*/DEEPSEEK_API_KEY=新密钥/' .env.prod"
   ```

### 方案3：重启后端服务

```bash
# 重启后端容器使配置生效
ssh -i ~/zhuangxiu-agent1.pem root@120.26.201.61 "cd /root/project/prod/zhuangxiu-agent && docker compose restart backend"

# 查看重启日志
ssh -i ~/zhuangxiu-agent1.pem root@120.26.201.61 "cd /root/project/prod/zhuangxiu-agent && docker compose logs backend --tail=20"
```

## 验证步骤

### 验证1：测试扣子API
```python
# 使用更新后的配置测试
python3 test_quote_analysis_full.py
```

### 验证2：查看日志确认
```bash
# 查看最新的分析日志
ssh -i ~/zhuangxiu-agent1.pem root@120.26.201.61 "cd /root/project/prod/zhuangxiu-agent && docker compose logs backend --tail=30 | grep -A5 -B5 '扣子站点API\|DeepSeek API'"
```

### 验证3：完整功能测试
1. 上传新的报价单图片
2. 检查分析状态是否变为"completed"
3. 确认返回的不是兜底数据

## 紧急备用方案

如果无法立即修复AI服务，可以临时启用本地OCR分析：

1. **修改配置**：在`.env.prod`中设置`USE_LOCAL_OCR=true`
2. **重启服务**：`docker compose restart backend`
3. **注意**：本地OCR分析质量较低，仅作为临时解决方案

## 预防措施

1. **定期检查API密钥有效期**：每月检查一次AI服务配置
2. **设置监控告警**：当AI分析失败率超过阈值时发送告警
3. **多备用服务**：配置多个AI服务提供商，提高可用性
4. **定期测试**：每周运行一次功能测试脚本

## 相关文件

- 配置文件：`/root/project/prod/zhuangxiu-agent/.env.prod`
- 测试脚本：`test_quote_analysis_full.py`
- 服务日志：`docker compose logs backend`
- 代码文件：`backend/app/services/coze_service.py`

## 负责人

- 后端开发：负责更新API配置
- 运维：负责重启服务和监控
- 测试：负责验证修复效果

## 时间预估

- 配置更新：15分钟
- 服务重启：5分钟
- 功能验证：10分钟
- 总计：30分钟

---

**状态**：待修复  
**优先级**：高  
**影响用户**：所有需要使用报价单分析功能的用户
