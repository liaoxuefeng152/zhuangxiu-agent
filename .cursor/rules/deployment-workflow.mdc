---
description: 开发部署流程 - 本地修改 → Git 提交 → 阿里云更新
alwaysApply: true
---

# 部署流程规范

## ⚠️ 部署前必读警告

**在开始任何部署操作之前，必须完整阅读本规则文件！**

**重要提醒**：每次部署前必须：
1. ✅ 确认当前在正确的分支（dev分支用于开发环境，main分支用于生产环境）
2. ✅ 确认已提交所有代码更改到Git
3. ✅ 确认已推送到远程仓库
4. ✅ 确认了解本次部署的影响范围
5. ✅ 确认已备份重要数据（如有需要）

**记住：如果改了后台代码，一定要更新到阿里云并重新构建、启动后端，否则修改不会生效。**

本项目使用**阿里云开发环境**（120.26.201.61），每次改动必须按以下顺序执行：

## 1. 在本地项目修改

- 所有代码改动先在本地项目 `/Users/mac/zhuangxiu-agent-backup-dev` 完成
- 修改后本地测试验证

## 2. 提交到 Git

```bash
git add .
git commit -m "描述本次修改"
git push
```

## 3. 更新阿里云开发环境

- **服务器项目目录**：`/root/project/dev/zhuangxiu-agent`
- **开发后端容器**：`decoration-backend-dev`（对应 docker-compose.dev.yml）
- **SSH 连接命令（固定使用密钥）**：`ssh -i ~/zhuangxiu-agent1.pem root@120.26.201.61`
- **更新步骤**：SSH 登录后拉取最新代码，重新构建并重启容器

```bash
# SSH 登录（必须使用指定密钥）
ssh -i ~/zhuangxiu-agent1.pem root@120.26.201.61

# 进入项目目录
cd /root/project/dev/zhuangxiu-agent

# 拉取最新代码
git pull

# 若有后端代码改动，需重新构建并重启
docker compose -f docker-compose.dev.yml build backend --no-cache
docker compose -f docker-compose.dev.yml up -d backend

# 或仅重启（未改 Dockerfile 时）
docker compose -f docker-compose.dev.yml restart backend
# 或：docker restart decoration-backend-dev
```

### ⚠️ 改后端代码后必须：提交 git → 更新阿里云 → 重启服务

**重要规则**：只要改了**后端代码**（`backend/` 下任何文件），必须完整做三步，否则线上不生效：

1. **提交到 Git**：`git add` 相关文件 → `git commit -m "..."` → `git push`
2. **更新到阿里云**：SSH 登录服务器后 `cd /root/project/dev/zhuangxiu-agent` → `git pull`（若有冲突可先 `git stash` 再 pull）
3. **重启后端服务**：`docker compose -f docker-compose.dev.yml build backend --no-cache && docker compose -f docker-compose.dev.yml up -d backend`（或仅 `restart backend`）

**操作顺序不可省略**：先 push，再在服务器上 pull 并重启。禁止直接在服务器改代码或跳过 Git 部署。

**强制要求**：**任何后端修复/改动，都必须更新到阿里云服务器并重启服务，否则修改不会生效！**

**重要提醒**：
- ✅ 每次更新代码后，必须先 `git push` 到远程仓库
- ✅ 后台有改动时，必须 SSH 到服务器执行 `git pull` 并重启后端服务
- ✅ **拉取代码后必须重新构建并重启容器**：`git pull` 后必须执行 `docker compose build` 和 `docker compose up -d`，否则容器中的代码不会更新
- ✅ SSH 固定使用：`ssh -i ~/zhuangxiu-agent1.pem root@120.26.201.61`
- ❌ **禁止**：直接修改阿里云服务器上的代码，或跳过 Git 推送直接部署
- ❌ **禁止**：只执行 `git pull` 而不重新构建和重启容器
- ❌ **严格禁止**：直接在生产环境服务器上修改代码、配置文件或任何项目文件
- ❌ **严格禁止**：绕过 Git 工作流直接在服务器上进行任何代码变更
- ✅ **必须遵守**：所有修改必须在本地开发环境完成，通过 Git 提交、推送，然后在服务器上拉取更新

## ⚠️ 生产环境修改特别警告

**绝对禁止直接在生产环境进行任何修改！**

1. **代码修改**：所有代码修改必须在本地开发环境完成，通过 Git 提交到 dev 分支，测试通过后合并到 main 分支
2. **配置修改**：配置文件修改必须在本地完成，通过 Git 提交和部署流程更新
3. **数据库修改**：数据库变更必须通过迁移脚本，在本地测试后通过部署流程执行
4. **紧急修复**：即使是紧急修复，也必须先在本地完成，通过 Git 提交，然后按照部署流程更新

**违规后果**：
- 直接在生产环境修改会导致代码版本不一致
- 可能引发生产环境故障
- 修改可能在下一次部署时被覆盖
- 无法追踪变更历史和责任人

**正确流程**：
1. 本地修改 → 2. Git 提交 → 3. 推送到远程仓库 → 4. 服务器拉取更新 → 5. 重新构建容器 → 6. 重启服务
