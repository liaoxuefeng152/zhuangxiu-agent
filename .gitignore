# 系统文件
.DS_Store

# 敏感配置文件：针对「后台环境变量」目录
# 先忽略所有.env相关文件
后台环境变量/.env
# 再精准忽略开发/生产/备份文件（避免通配符覆盖.example）
后台环境变量/.env.dev
后台环境变量/.env.prod
后台环境变量/.env.backup
后台环境变量/.env.prod.backup
后台环境变量/.env.dev_backup
# 明确保留模板文件（! 表示不忽略）
!后台环境变量/.env.example

# 其他无需上传的文件（按需添加，如日志、缓存）
node_modules/
dist/
logs/
.pydeps/
__pycache__/
*.pyc
**/__pycache__/
**/*.pyc
.swc/

# 测试报告和日志
*.log
test-*-report-*.json
acceptance-test-output.log

# IDE配置
.idea/
.vscode/
*.swp
*.swo
*~

# 临时文件
*.backup
*.bak
*.tmp

# 环境配置文件（所有 .env 相关，除了模板文件 .env.example）
.env
.env.*
!*.env.example
!scripts/.env.example.contract_check

# 证书文件夹（敏感）
cert/

# 备份文件
$BACKUP_FILE
*_backup

# 前端环境文件（如果不需要提交）
frontend/.env.development*
frontend/.env.production*
# 防止包含"dev"的配置文件被提交到main分支
# 开发环境配置文件
*dev*
*DEV*
# 但允许开发环境文档和测试文件
!docs/*dev*
!tests/*dev*
# 不允许开发环境配置文件
config/dev/
docker-compose.dev*
.env.dev*
