# 报价单分析功能排查步骤与日志命令

## 问题描述
报价单分析功能失败，前端显示"分析失败"或"AI分析服务异常"。

## 问题根源分析
**这是后台问题** - 扣子智能体配置了工具调用功能，导致返回`tool_request`和`tool_response`类型的数据块，而不是直接返回JSON格式的分析结果。

## 排查步骤

### 1. 检查后端日志
```bash
# 查看最新的后端日志
ssh -i ~/zhuangxiu-agent1.pem root@120.26.201.61 "cd /root/project/dev/zhuangxiu-agent && docker compose -f docker-compose.dev.yml logs backend --tail=100"

# 查看特定时间段的日志
ssh -i ~/zhuangxiu-agent1.pem root@120.26.201.61 "cd /root/project/dev/zhuangxiu-agent && docker compose -f docker-compose.dev.yml logs backend --since='2026-03-17T00:00:00'"

# 查看包含"扣子"关键字的日志
ssh -i ~/zhuangxiu-agent1.pem root@120.26.201.61 "cd /root/project/dev/zhuangxiu-agent && docker compose -f docker-compose.dev.yml logs backend | grep -i '扣子' | tail -50"

# 查看包含"quote"或"报价单"关键字的日志
ssh -i ~/zhuangxiu-agent1.pem root@120.26.201.61 "cd /root/project/dev/zhuangxiu-agent && docker compose -f docker-compose.dev.yml logs backend | grep -i 'quote\|报价单' | tail -50"
```

### 2. 检查扣子智能体连接状态
```bash
# 本地测试扣子智能体连接
cd /Users/mac/zhuangxiu-agent && python3 -c "
import httpx
import asyncio
import json

async def test():
    site_url = 'https://9n37hmztzw.coze.site'
    site_token = 'eyJhbGciOiJSUzI1NiIsImtpZCI6ImIxYmFkYTkxLTYyMjctNDAyYi1iZTMwLTU4ZTMxODQzYjJjYiJ9..HfN-fqpyZiVyDS_8RndLNgcmmcwhY6kGbHAeuidFOTDGhLXFVdPuf1WrEhd_zYzbjL2SXbx8Gg6acaUu8FQCJDQQQd46NeY_NoIQQti1Gh5ZXVry7K6qtxCLxkX46MzTZ_sP1PkqzgCRMFJMjLKZ3wAEBkALLxkR82uzdjYUoiRz6pFhm6Rhvwtk-3cxLFmwr5w6vfeRFCBQyBcti_Uks8JaKjp6nvqg_cseYmVrtym2Sp0bcDFUQx5F2ft6qm-4g0cT-n2DCFSyWaEVl_lvf09NQU43gU6ucDOpwDU4gduDss1en-OMfWIEfa7-u9_gXTn2AcQvwpNb7U4ZzCH7zg'
    project_id = '7603691852046368804'
    
    api_url = f'{site_url.rstrip(\"/\")}/stream_run'
    print(f'测试URL: {api_url}')
    
    data = {
        'content': {
            'query': {
                'prompt': [
                    {
                        'type': 'text',
                        'content': {
                            'text': '测试连接，请回复\"连接成功\"'
                        }
                    }
                ]
            }
        },
        'type': 'query',
        'session_id': 'test_session_123',
        'project_id': project_id,
        'config': {'recursion_limit': 25},
    }
    
    headers = {
        'Authorization': f'Bearer {site_token}',
        'Content-Type': 'application/json'
    }
    
    try:
        async with httpx.AsyncClient(timeout=30.0) as client:
            async with client.stream('POST', api_url, json=data, headers=headers) as response:
                print(f'响应状态: {response.status_code}')
                if response.status_code == 200:
                    print('✅ 连接成功!')
                    chunks = []
                    async for line in response.aiter_lines():
                        line = (line or '').strip()
                        if not line or line == 'data: [DONE]':
                            continue
                        if not line.startswith('data:'):
                            continue
                        json_str = line[5:].strip()
                        try:
                            data_chunk = json.loads(json_str)
                            print(f'收到数据块类型: {data_chunk.get(\"type\", \"unknown\")}')
                            chunks.append(json_str)
                            if len(chunks) >= 3:
                                break
                        except json.JSONDecodeError:
                            continue
                    if chunks:
                        print(f'✅ 成功收到{len(chunks)}个数据块')
                    else:
                        print('⚠️ 没有收到有效数据块')
                else:
                    print(f'❌ 连接失败: {response.status_code}')
    except Exception as e:
        print(f'❌ 请求异常: {e}')

asyncio.run(test())
"
```

### 3. 测试报价单分析功能
```bash
# 使用测试脚本测试报价单分析
cd /Users/mac/zhuangxiu-agent && python3 test_coze_quote_analysis.py

# 或者直接运行测试
cd /Users/mac/zhuangxiu-agent && python3 -c "
import asyncio
from backend.app.services.coze_service import coze_service

async def test():
    # 使用一个测试图片URL
    test_image_url = 'https://example.com/test-quote.jpg'
    print(f'测试报价单分析: {test_image_url}')
    
    result = await coze_service.analyze_quote(test_image_url, user_id=123)
    if result:
        print('✅ 报价单分析成功!')
        print(f'结果类型: {type(result)}')
        print(f'结果字段: {list(result.keys()) if isinstance(result, dict) else \"N/A\"}')
        print(f'风险评分: {result.get(\"risk_score\", \"N/A\")}')
        print(f'总价: {result.get(\"total_price\", \"N/A\")}')
        print(f'高风险项目数: {len(result.get(\"high_risk_items\", []))}')
        print(f'建议数: {len(result.get(\"suggestions\", []))}')
    else:
        print('❌ 报价单分析失败')

asyncio.run(test())
"
```

### 4. 检查修复后的代码
```bash
# 查看修复后的_extract_content_from_stream方法
cd /Users/mac/zhuangxiu-agent && grep -n "_extract_content_from_stream" backend/app/services/coze_service.py -A 50

# 测试修复方法
cd /Users/mac/zhuangxiu-agent && python3 fix_coze_stream_extraction.py
```

### 5. 部署修复到阿里云
```bash
# 1. 提交修复代码到Git
cd /Users/mac/zhuangxiu-agent
git add backend/app/services/coze_service.py
git commit -m "fix: 修复扣子智能体流式响应提取问题，正确处理tool_request和tool_response类型"
git push origin dev

# 2. 部署到阿里云开发环境
ssh -i ~/zhuangxiu-agent1.pem root@120.26.201.61 "cd /root/project/dev/zhuangxiu-agent && git checkout dev && git pull origin dev && docker compose -f docker-compose.dev.yml build backend --no-cache && docker compose -f docker-compose.dev.yml up -d backend"

# 3. 验证部署
ssh -i ~/zhuangxiu-agent1.pem root@120.26.201.61 "cd /root/project/dev/zhuangxiu-agent && docker compose -f docker-compose.dev.yml ps"

# 4. 查看部署后的日志
ssh -i ~/zhuangxiu-agent1.pem root@120.26.201.61 "cd /root/project/dev/zhuangxiu-agent && docker compose -f docker-compose.dev.yml logs backend --tail=50"
```

### 6. 验证修复效果
```bash
# 1. 在前端上传报价单图片进行测试
# 2. 查看后端日志确认分析成功
ssh -i ~/zhuangxiu-agent1.pem root@120.26.201.61 "cd /root/project/dev/zhuangxiu-agent && docker compose -f docker-compose.dev.yml logs backend | grep -i '扣子站点提取chunk\|AI分析成功\|报价单分析' | tail -20"

# 3. 检查数据库中的分析结果
ssh -i ~/zhuangxiu-agent1.pem root@120.26.201.61 "cd /root/project/dev/zhuangxiu-agent && docker compose -f docker-compose.dev.yml exec backend python -c \"
import sys
sys.path.append('/app')
from app.db.session import SessionLocal
from app.models.quote import QuoteAnalysis
db = SessionLocal()
try:
    analyses = db.query(QuoteAnalysis).order_by(QuoteAnalysis.created_at.desc()).limit(5).all()
    for a in analyses:
        print(f'ID: {a.id}, 用户ID: {a.user_id}, 创建时间: {a.created_at}, 风险评分: {a.risk_score}, 总价: {a.total_price}')
finally:
    db.close()
\""
```

## 问题解决方案

### 已实施的修复
1. **修复`_extract_content_from_stream`方法**：
   - 正确处理`tool_request`和`tool_response`类型，过滤掉这些工具调用数据块
   - 正确处理`answer`类型，提取实际的分析内容
   - 合并多个`answer`数据块中的JSON片段

2. **增强错误处理**：
   - 当扣子智能体返回工具调用说明时，返回兜底分析数据
   - 增加调试日志，便于问题排查

### 根本原因
扣子智能体配置了工具调用功能，导致：
1. 智能体返回`tool_request`和`tool_response`类型的数据块
2. 代码中的`_extract_content_from_stream`方法没有正确处理这些类型
3. 导致无法提取到实际的分析内容，返回空结果

### 预防措施
1. **监控日志**：定期检查扣子智能体调用日志
2. **测试脚本**：维护测试脚本，定期验证扣子智能体连接和分析功能
3. **兜底机制**：确保AI分析失败时有合理的兜底数据返回
4. **错误处理**：增强错误处理，提供更清晰的错误信息

## 快速诊断命令

```bash
# 一键诊断脚本
cd /Users/mac/zhuangxiu-agent && cat > diagnose_quote_analysis.sh << 'EOF'
#!/bin/bash
echo "=== 报价单分析功能诊断 ==="
echo "1. 检查本地代码..."
grep -n "_extract_content_from_stream" backend/app/services/coze_service.py -A 5 | head -20
echo ""
echo "2. 测试扣子连接..."
python3 -c "
import httpx
import asyncio
import json

async def test():
    site_url = 'https://9n37hmztzw.coze.site'
    site_token = 'eyJhbGciOiJSUzI1NiIsImtpZCI6ImIxYmFkYTkxLTYyMjctNDAyYi1iZTMwLTU4ZTMxODQzYjJjYiJ9..HfN-fqpyZiVyDS_8RndLNgcmmcwhY6kGbHAeuidFOTDGhLXFVdPuf1WrEhd_zYzbjL2SXbx8Gg6acaUu8FQCJDQQQd46NeY_NoIQQti1Gh5ZXVry7K6qtxCLxkX46MzTZ_sP1PkqzgCRMFJMjLKZ3wAEBkALLxkR82uzdjYUoiRz6pFhm6Rhvwtk-3cxLFmwr5w6vfeRFCBQyBcti_Uks8JaKjp6nvqg_cseYmVrtym2Sp0bcDFUQx5F2ft6qm-4g0cT-n2DCFSyWaEVl_lvf09NQU43gU6ucDOpwDU4gduDss1en-OMfWIEfa7-u9_gXTn2AcQvwpNb7U4ZzCH7zg'
    project_id = '7603691852046368804'
    
    api_url = f'{site_url.rstrip(\"/\")}/stream_run'
    print(f'测试URL: {api_url}')
    
    data = {
        'content': {
            'query': {
                'prompt': [
                    {
                        'type': 'text',
                        'content': {
                            'text': '测试连接'
                        }
                    }
                ]
            }
        },
        'type': 'query',
        'session_id': 'test_session_123',
        'project_id': project_id,
        'config': {'recursion_limit': 25},
    }
    
    headers = {
        'Authorization': f'Bearer {site_token}',
        'Content-Type': 'application/json'
    }
    
    try:
        async with httpx.AsyncClient(timeout=10.0) as client:
            response = await client.post(api_url, json=data, headers=headers)
            print(f'响应状态: {response.status_code}')
            if response.status_code == 200:
                print('✅ 扣子连接正常')
            else:
                print(f'❌ 扣子连接异常: {response.status_code}')
    except Exception as e:
        print(f'❌ 连接测试异常: {e}')

asyncio.run(test())
"
echo ""
echo "3. 检查阿里云日志..."
echo "请手动执行: ssh -i ~/zhuangxiu-agent1.pem root@120.26.201.61 \"cd /root/project/dev/zhuangxiu-agent && docker compose -f docker-compose.dev.yml logs backend --tail=20\""
EOF
chmod +x diagnose_quote_analysis.sh
./diagnose_quote_analysis.sh
```

## 总结
报价单分析功能失败的原因是**后台问题**：扣子智能体配置了工具调用功能，导致返回`tool_request`和`tool_response`类型的数据块，而代码没有正确处理这些类型。修复方法是更新`_extract_content_from_stream`方法，过滤掉工具调用数据块，正确提取`answer`类型中的分析内容。

修复后需要：
1. 提交代码到Git
2. 部署到阿里云开发环境
3. 重启后端服务
4. 验证修复效果
